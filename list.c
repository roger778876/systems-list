#include <stdio.h>
#include <stdlib.h>

struct node {char *s, struct node *next};

void print_list(struct node *){
  printf("[");
  while (node.next != 0){
    printf("%s, ", node.s);
    node++;
    
  }
  printf("]\n");
}

int main(){
    struct node n1 = 
}
